services:
  postgres:
    image: postgres:16-alpine
    container_name: goatwedding-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: goatwedding
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d goatwedding"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
